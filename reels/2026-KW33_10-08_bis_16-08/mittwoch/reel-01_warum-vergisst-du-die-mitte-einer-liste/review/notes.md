# Review-Notizen

Inhalt und Quellen wurden bei Erstellung manuell geprüft. Externe Bilder und Voice-over liegen noch nicht vor. Deshalb sind visuelle Zuordnung, echte Audio-Messung, 100-%-Word-Sync, Finalizer und Render noch nicht als bestanden markiert. `npm test` und Repo-CLI-Validatoren wurden nicht ausgeführt.
