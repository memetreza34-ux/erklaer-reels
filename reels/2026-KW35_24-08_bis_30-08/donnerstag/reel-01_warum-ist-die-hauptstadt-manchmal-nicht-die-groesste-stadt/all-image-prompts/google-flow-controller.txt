GOOGLE FLOW — HARD SERIAL LOCK — EXACTLY ONE IMAGE GENERATION AT A TIME

THIS FILE CONTROLS EXECUTION ONLY. NEVER COMBINE THE VISUAL PROMPTS.

NON-NEGOTIABLE CONCURRENCY RULE:
There may be at most ONE active, pending, queued, or running image-generation job at any time.
Never launch a second image generation while the previous one is generating, pending, rendering, saving, downloading, renaming, or being verified.
Never issue two or more image-generation actions in the same agent turn, tool batch, action batch, or parallel call.
If the platform offers parallel calls, batch generation, queues, multi-image generation, variants, grids, or contact sheets: DO NOT USE THEM.
If you cannot prove that the previous image is completely finished, WAIT. If waiting is not possible, STOP instead of launching another image.
If more than one generation accidentally starts, cancel every later job and keep only the earliest active job.

SERIAL STATE MACHINE:
READY -> GENERATING -> VERIFYING -> READY
Only in READY may you open the next prompt file.
When generation starts, immediately enter GENERATING and keep a lock on that image number.
Stay in GENERATING until the image is visibly complete and the generation UI shows no active/pending job for it.
Then enter VERIFYING: save/download if needed, rename to the exact Bild NN.png filename, visually check the result, and confirm the filename.
Only after VERIFYING is fully complete may the lock be cleared and the state return to READY.

IMPORTANT AGENT-TURN BARRIER:
After you invoke one image-generation action, do not invoke another image-generation action in that same turn/action cycle. End the generation action and wait for the actual completed result/UI state before continuing.
Do not pre-read the next prompt while the current image is still in GENERATING or VERIFYING.
Do not plan or queue future generations in advance.

VISUAL PROMPT RULE:
Each file in `image-prompts/` is already the final high-quality visual prompt in the approved legacy editorial structure.
Use it verbatim. Do not rewrite, summarize, shorten, merge, simplify, or add workflow text to it.
Workflow labels and filenames must never appear in the generated image.

START NOW:
1. State must be READY.
2. Open ONLY `image-prompts/Bild 00.txt`.
3. Read ONLY that file.
4. Generate exactly ONE image from it.
5. Enter GENERATING and wait until it is completely finished.
6. Enter VERIFYING, rename/save it exactly as `Bild 00.png`, visually verify it, and verify the filename.
7. Only then clear the lock, return to READY, and open `image-prompts/Bild 01.txt`.

CONTINUE WITH THE SAME HARD BARRIER:
Bild 01.txt -> Bild 01.png
Bild 02.txt -> Bild 02.png
Bild 03.txt -> Bild 03.png
Bild 04.txt -> Bild 04.png
Bild 05.txt -> Bild 05.png
Bild 06.txt -> Bild 06.png
Bild 07.txt -> Bild 07.png
Bild 08.txt -> Bild 08.png
Bild 09.txt -> Bild 09.png
Bild 10.txt -> Bild 10.png
Bild 11.txt -> Bild 11.png
Bild 12.txt -> Bild 12.png
Bild 13.txt -> Bild 13.png
Bild 14.txt -> Bild 14.png
Bild 15.txt -> Bild 15.png
Bild 16.txt -> Bild 16.png
Bild 17.txt -> Bild 17.png
Bild 18.txt -> Bild 18.png

Never ask for Go/Weiter/OK between images, but waiting for actual completion is mandatory. Speed is never more important than strict serial execution and image quality.
